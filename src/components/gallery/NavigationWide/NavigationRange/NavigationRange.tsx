import './navigationRange.scss';

const NavigationRange = ({ length } : { length: number }) => {
    // next time...

    return <div 
        className='navigation__range'
    >

    </div>
}

export default NavigationRange;